#Encryption function
def encrypt_credit_card(card_number,key):
    cipher = AES.new(key, AES.MODE_ECB)
    padded_data = pad(card_number.encode(),AES.block_size)
    encrypted_data = cipher.encrypt(padded_data)
    return encrypted_data
